/**
 * Renders an MUI chip used in the searchable tags of each `MediaCard`
 * @interface
 */
export declare interface Tags {
    /**
     * Tag content
     * @type {string}
     */
    children: string
}